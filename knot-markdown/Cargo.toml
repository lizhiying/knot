[package]
name = "knot-markdown"
version = "0.1.0"
edition = "2021"
description = "Markdown parser for knot ecosystem - builds heading trees from markdown text"

[dependencies]
pulldown-cmark = "0.10"
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
